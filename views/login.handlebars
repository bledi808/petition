<header>
    <div class="logo-container">
        <div id="logo" style="font-size:larger">
            Meta-Petition ✍🏼
        </div>
    </div>       
    <div style="margin-top:4px">
        <form action='/register'>
            <button id="nav-button" style="border:none; background-color: none; font-size:large">Register</button> 
            {{!-- {{>csrf}}     --}}
        </form>
    </div>
</header>
<div class="petition-container">
    <form method="POST" id="register-container">
        <p style="margin:0 0 1% 2%; text-shadow: 1px 1px 1px darkgrey; color: midnightblue">
            Login below
        </p>
            <div class="form-layout">   
                <div class="input-fields">
                    <input  class="reg-input" type="email" id="surname" name="email" placeholder="Email">
                    <input  class="reg-input" type="password" id="surname" name="password" placeholder="Password">
                    {{>csrf}}
                <div id="reg-actions">
                    <button name="submit" id="submit-reg">Login</button>
                    <span id="already-reg">
                        Register <a href="/register" style="text-decoration: none; color: tomato">here</a> instead!
                    </span>
                </div> 
            </div> 
        </div>         
    </form>
    {{#if empty}}
    <p style="color: tomato; margin-top:1%">These credentials appear incorrect!</p>  
    {{/if}}
</div>
