<div class="register-form" style="margin:10%">
        <form method="POST">
            <h4 style="margin-bottom:1%">Login below:</h4>
                <div class="form-layout">   
                <div class="input-fields">
                    <span></br>Email</span></br>
                        <input type="text" id="surname" name="surname" placeholder="type email..." style="margin:1% 0">
                    <span></br>Password</span></br>
                        <input type="text" id="surname" name="surname" placeholder="type password..." style="margin:1% 0">
                <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <div><button name="submit" id="submit">Log in</button></div>     
        </form>
        {{#if empty}}
        <p style="color: tomato; margin-top:1%">These credentials appear incorrect!</p>  
        {{/if}}
    </div>