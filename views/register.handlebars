
<header>
    <div class="logo-container">
        <div id="logo" style="font-size:larger">
            ✍🏼Meta-Petition 
        </div>
    </div>       
    <div style="margin-top:4px">
        <form action='/login'>
            <button id="nav-button" style="border:none; background-color: none;font-size:large; ">Login</button> 
            {{>csrf}}    
        </form>
    </div>
</header> 
<div class="petition-container">
    <div class="intro">
        <p> Do you like filling in forms?</p>
        <p> Do you love to type inside neat little boxes?</p>   
        <p>Does the thought of a 'census' make you lose your senses?</p>         
    </div>
    <form id="register-container" method="POST">
        <p style="margin:0 0 1% 2%; text-shadow: 1px 1px 1px darkgrey; color: midnightblue">
            Register with your details 
        </p>
        <div class="form-layout">   
            <div class="input-fields">
                <input class="reg-input" type="text" id="firstname" name="firstname" placeholder="Name" autocomplete="off">
                <input class="reg-input" type="text" id="surname" name="surname" placeholder="Surame" autocomplete="off">
                <input  class="reg-input" type="email" id="surname" name="email" placeholder="Email" autocomplete="off">
                <input  class="reg-input" type="password" id="surname" name="password" placeholder="Password" autocomplete="off">
                {{>csrf}}
            <div id="reg-actions">
                <button name="submit" id="submit-reg">Register</button>
                <span id="already-reg">
                    Login <a href="/login" style="text-decoration: none; color: tomato">here</a> instead!
                <span>
            </div>
                {{#if empty}}
                    <p class="error"> Please populate all fields to register</p>  
                {{/if}}
                {{#if email}}
                    <p class="error"> Email is already taken, try another</p>  
                {{/if}}
            </div>
        </div>                
    </form>
    <div class="outro">
    <p>Gently nodding 'yes' to the above?
        You are not alone!</p>
        {{!-- <p>Sign up to find others in your city just like you!</p> --}}
    </div>
 
</div>

